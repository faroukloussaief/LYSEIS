// sample_clean.js — fixture for Lyseis unit tests
// This file should produce zero findings

function greet(name) {
    return "Hello, " + name + "!";
}

const PI = 3.14159;

function add(a, b) {
    return a + b;
}

module.exports = { greet, add, PI };
